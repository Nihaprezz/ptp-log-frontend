import React from "react";

const SkipTraceTable = (props) => {

    return (
        <div>
            <h1>{props.skipType}</h1>
            Skip Trace table will go here
        </div>
    )
}

export default SkipTraceTable;